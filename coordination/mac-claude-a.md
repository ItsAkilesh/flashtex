# mac-claude-a handoff

## Durable checkpoint (docs/context-checkpoints.md) — 2026-09-12T09:35Z

- Task: FT-003 rev 7 (acked). Branch `agent/mac-claude-a/mac-shell` @ 92a052c (pushed), worktree /Users/jay3332/Projects/flashtex (main checkout), dirty: none besides untracked scratch. Main reviewed through 745f327 (b587385 integrated crates/pdf exact API; queue steps: 1 remeasure helper — done; 2 exact PDF export in app — waiting on mac-pdf from-v2 + preview-v2 live).
- Completed today (370/370 native tests with seven real helpers; xcodebuild OK): preview render fixes; run-loop delivery; byte-equal text; #26 exact cache; @Observable + scoped panes; preview-controller adapter (hold-until-preview) + helper-route save (export) ; FastJSON (3.8x); worker auto-relaunch (bounded); 13 lanes integrated with their parent diffs: preview-cache, editor-diagnostics, document-files (+crates/project-files helper binary), completion (+live-helper), pairing-ui, nearby-transport (+events, pairs.json v2), navigation, ai-review (assistant-context c8a3e1b/c93bf0d), editor-accessibility, validation (PASS 119/119 report), preview-v2 (zero-pixel parity 22 pages).
- Evidence: docs/evidence/typing-bench-2026-09-12T091914Z.md (quiet direct: fixture 23/36, demo 31/40, 60 KB 163/273 @30 ms, 118/146 burst p50/p99), typing-bench-2026-09-12T084034Z.md + T090250Z + earlier helper-route runs; tools/native-validation/mac-live/reports/20260912T090452Z.md; docs/evidence/mac-preview-v2-parity-2026-09-12.md. Issue #2 comments through 5645001677; #26 fixed; #28 assigned to mac-pdf; completed-snapshot proposal ACKed with required fields (5644981151).
- Running child sessions: a1798a96df2c0bacc render-pipeline (geometry vs MacTeX), a4c50016b414418ad nearby-client (error codes), a665565ecb0f11ace mac-pdf (#28 then from-v2, base main ≥ b587385), a93c86d3bd8db3ed3 preview-v2 (live v2 negotiation with render-pipeline), a305a521269404799 mac-bridge-recovery (bridge/ledger relaunch), a4c2c20d016d17c6d mac-multifile (ProjectDocuments via helper). Finished/merged: editor-diagnostics, preview-cache, typing-bench, document-files, completion, pairing-ui, nearby-transport, navigation, ai-review, editor-accessibility, validation, visual-oracle (10e80be ready_for_integration, Rust/harness only — Commander integrates), standby orchestrator-jaysen-opus (idle). Monitor b227i7j0p watches main/next/#2.
- Pending: none uncertain. Next: integrate lanes as they land; wire File > Export through the exact route when from-v2 lands; recommend to Commander promoting flashtex-render to main as an optional producer so the default preview face can be Latin Modern (user ask).
- Rules in force: user is primary Git author on this Mac (jay3332) with truthful trailers; Claude Max 20x authorized for parent + subagents, no purchases/overages; Codex on hold (15 % weekly floor); parent retains ShellModel*/ContentView/PreviewView/FlashTeXMacApp; never edit crates/font-engine, paragraph-layout, math-layout (Daniel); MacTeX 2026 full is oracle only; never steal keyboard focus; kill helpers by pid only; Commander is orchestrator-astra — no takeover. No context-usage telemetry in this session; compaction is host-managed.

Agent / task / branch: mac-claude-a (Claude Code on mac-m1max-a, user-directed) / FT-003 rev 3 native editor and preview quality (rev 1–2 complete) / `agent/mac-claude-a/mac-shell`
State: FT-003 rev 5 in progress; integration candidate f1bf50a (167/167 tests with real compiler/pdf/bridge/edit-ledger; #19 fixed; layout-capabilities consumer gate 2; Latin Modern vendored under apps/mac/Fonts) (rev 2 at fd2a26e; increments 1–9: shell, transport, capture insertion, real-compiler e2e, PDF export, caret sync); supervising mac-pdf (FT-009) and mac-validation (FT-010) subagents
Owned paths: `apps/mac/`, `coordination/mac-claude-a.md`, `coordination/agents/mac-claude-a.json`
Main integrated through: 09031ef (merged at ac23a9a); reviewed through 4d17c62 (rev 3 dispatch)
Ready behavior: increment 20 (48780a8): transfer-v1 bridge client + ledgered exactly-once insertion vs bridge b5ca96b (59/59 tests incl. RealBridgeTests); FLASHTEX_LOG sink; packaging install/dmg/launch-check merged. Increments 15–19 (803a964): .tex open/save with dirty tracking; FlashTeX.app packaging (scripts/make-app.sh; bundles compiler + pdf writer; bundled compiler auto-attaches; evidence docs/evidence/flashtex-app-bundle-2026-09-12.png); preview draws Times-Roman (was New York — my bug behind the crowded words); U+2500 fraction bars drawn as rules in preview and CoreGraphics export (issue #9); math evidence docs/evidence/mac-shell-math-times-2026-09-12.png with compiler de1020c; 47/47 tests. Increments 12–14 (837dd33): fit-to-width preview scaling, page-level caret scroll, hit rects keyed by item index; multi-page evidence docs/evidence/mac-shell-demo-3-pages-2026-09-12.png (Codex-authored demo.tex, 3 pages, 0 diagnostics, 55 ms). Increment 11 (4d93a7f): Commander findings fixed — complete oversized lines rejected (LineSplitter made linear-time), EOF trailing bytes are a violation, compile_result correlated by id + project_id + revision (44/44 tests, each failure reproduced by a labeled fake-worker directive). Codex-authored 3-page demo.tex merged. Increment 10 (b41ef87): regular activation policy (window appears from non-GUI launch), labeled toolbar toggles, FLASHTEX_AUTOATTACH/FLASHTEX_SEED_FILE hooks, screenshot evidence docs/evidence/mac-shell-real-compiler-2026-09-12.png with the real compiler (WORKER badge, recovered status, diagnostics, inline underlines). Finding for FT-005: preview words overlap due to placeholder glyph widths in the compiler (reported on issue #1). Increments 8–9 (rev 3 acceptance): export always white regardless of dark preview (fixed a regression in the merged CoreGraphics export), second export path through the FT-009 Rust writer (`flashtex-pdf --verify`, PDFKit-verified), diagnostics panel never hidden with recovery notes + banner counts, inline underline marks in the editor with recovery tooltips (rebased across edits or dropped), keyboard-shortcut table; 41/41 tests with FLASHTEX_COMPILER and FLASHTEX_PDF; FT-012 check_runtime.py validates a real compiler transcript (exit 0). Increment 7 (rev 2 acceptance): SourceMapping refuses stale offsets unless rebased+verified (tests), debounced auto-compile with one-in-flight coalescing, measured latency 0.5–9 ms/request with the real compiler (10-edit burst → 2 requests), 34/34 tests. Native build command: `cd apps/mac && xcodebuild -scheme FlashTeXMac -destination 'platform=macOS' build` → BUILD SUCCEEDED. Increment 5–6 (subagent branches merged): File > Export PDF… (CoreGraphics/CoreText, white page by default, dark only if toggled; PDFKit-verified page sizes/text) and caret→preview highlight with a byte-exact multi-page sample incl. diagnostics; 28/28 tests with the real compiler. Increment 4: END-TO-END with the real FT-002 compiler — shell launches crates/compiler binary (built from origin/agent/claude/compiler-foundation 29221d8 in a scratch worktree), compiles a multi-byte document, receives per-word items + diagnostics, and every span navigates to the exact text (RealCompilerTests, gated on FLASHTEX_COMPILER; 20/20 with binary, 19 + 1 skipped without). Increment 3 adds Mac-side capture review/insertion (pin destination anchor, proposal review sheet, one undoable edit, duplicate capture_id suppression, rebase-or-reselect on edits) with 19/19 tests. Increment 2 adds runtime-v1 JSON Lines worker transport (attach executable, ⌘B compile, WORKER/FIXTURE badge, stale-revision guard, error/violation/exit reporting) verified against a Python test double; 13/13 tests. Increment 1: Swift package builds (`swift build`, `xcodebuild -scheme FlashTeXMac -destination 'platform=macOS' build` → BUILD SUCCEEDED); editor + fixture preview with explicit FIXTURE status; clicking preview text selects the UTF-8 source range converted to UTF-16; diagnostics "Go to source"; dark preview toggle. 6/6 tests pass (`swift test`). See `apps/mac/README.md`.
Incomplete behavior: no real Rust worker to attach yet (needs FT-002 binary path); no PDF; no visual screenshot evidence (agent terminal lacks Screen Recording permission — user to eyeball the running app); no reverse sync.
Interface changes and required consumer actions: none. Consumes runtime-v1 as published; unknown item kinds are tolerated.
Validation: `cd apps/mac && swift build && swift test (13/13) && xcodebuild -scheme FlashTeXMac -destination 'platform=macOS' build → BUILD SUCCEEDED` on Xcode 26.3 / Swift 6.2.4, 2026-09-12T04:24Z.
Companion verification for aarush-macbook (requested): FAILS as committed — project.pbxproj corrupt since 8837135; Swift compiles after a worktree-only repair; tests have no target. Full report: docs/evidence/companion-capture-build-verification.md. No gh CLI here, so this handoff is the channel.
Commander items acknowledged from origin/agent/commander/coordination-tools 25a92a9 (not yet on main): FT-007 is Commander-owned (crates/bridge) — I will NOT build a transfer crate; FT-009 mac-pdf and FT-010 mac-validation dispatched as my subagents — both spawned in isolated worktrees on branches agent/mac-pdf/pdf-output and agent/mac-validation/native-verification; queue step "Live compiler integration" — transport/revision ordering/real-output-changes done; remaining: debounced auto-compile, cancellation/coalescing, measured latency, and refusing stale-offset navigation without a validated edit mapping (next).
Fixture issue for FT-001 owner: protocol/fixtures/compile-result.json item text "Hello FlashTeX." (15 bytes) vs range 0..<14; real compiler spans are exact.
Needs from others: Commander to integrate FT-002 (29221d8) and this branch; once both are on main, `cargo build --release` in crates/compiler then File > Attach Built Compiler (⌘⇧K) in the shell gives the live path. Interop note for FT-002 owner: none required — protocol matched on first try. Offer: this Mac can run xcodebuild on origin/agent/aarush-macbook/companion-capture if the Commander wants build evidence for FT-004.
Proposal branch agent/mac-claude-a/fontmetrics 30a14a6 is SUPERSEDED: compiler de1020c adopted its own Core-14 metrics; branch left for reference only. Children: mac-pdf FT-009 ready at agent/mac-pdf/pdf-output a0855dd (parent re-verified 20/20, sips opens PDF; registered/acked); mac-validation FT-010 ready at agent/mac-validation/native-verification b9c06ae (20 PASS/1 FAIL=fixture end_byte/6 INFO; Screen Recording works, Accessibility blocked; registered/acked); Codex-driver (Sonnet + headless codex exec, user-authorized light use of ChatGPT Plus) producing apps/mac/Samples/demo.tex on agent/mac-claude-a/demo-sample.
Issue #9 (compiler vs crates/pdf math/fonts): RESOLVED on the PDF side — mac-pdf c0f3837 (24/24; Symbol-font runs, rules, zero warnings on the repro); Mac preview/export at 803a964 use the same rule convention; contract proposal (optional font field, rule kind, pdf_warnings) posted for the Commander. Issue #10 (user request): BasicTeX installed by the user at /Library/TeX/texbin as a reference oracle only; mac-validation resumed to build tools/native-validation/oracle_compare (pdflatex vs compiler+pdf). Children (after the user accidentally stopped five workers; WIP preserved and relaunched from exact commits): RUNNING — render-pipeline (Opus, from b2d4708), math-layout FT-020 r3 (from db90047), paragraph-layout FT-019 r4 (from c471684), visual-oracle FT-017 r2 (from 78b9a64), layout-capabilities gate 3 (from merged f1bf50a), typing-bench (new), preview-v2 (new), nearby-client (new). DONE — completion, packaging r5 (merged), nearby, proposal-preview, accessibility, bridge fixes, pdf capabilities (4bd8c2e), document-style, project-files (#18 fixed d92db37), vector-graphics, bibliography, diagnostic-explanations, font-engine FT-018 r3 (f418238), native-e2e r5 (51c8c77). Oracle: BasicTeX removed; MacTeX (full) installing at user request; workers reuse recorded references meanwhile. Codex offload: user-authorized with 15% weekly floor; two Sonnet drivers declined on relayed-authorization grounds; direct run was interrupted — on hold pending user confirmation.
Next action (previous): queue step "Live compiler integration" remainder — edit mapping so navigation never uses stale byte offsets unless rebased through recorded edits (tests), debounced auto-compile with coalescing, measured edit-to-preview latency; then queue step "Native editor and preview quality". Collect mac-pdf/mac-validation results and record them here.
Peer revisions reviewed and adaptations: origin/main d685879 (no change); origin/agent/claude/compiler-foundation 25fe5c4 (FT-002 ack only) → none; origin/agent/claude/compiler-foundation 29221d8 (FT-002 compiler; runtime-v1 JSON Lines; per-word items) → action: attached and verified end-to-end, added auto-discovery of the built binary. origin/agent/aarush-macbook/companion-capture eb1c2fc (companion transport prints capture_submit lines to stdout; handoff says the agent cannot run xcodebuild and requests verification here) → action: run xcodebuild on that branch from this Mac. Note for FT-007: the Mac shell will need a listener for capture_submit; not started.
Resource: session runs on user's Claude Max at explicit user direction (override of Codex-only note on openai-mac-plus-ft003); no API spend; usage unknown.
Commit identity: jay3332 per AGENTS.md mac-m1max-a exception; Commit-Executor: git via Claude Code (Cursor CLI not logged in here).
Updated: 2026-09-12T09:35:00Z
