# mac-ios-app handoff

## Durable checkpoint — 2026-09-12T19:15Z (actual `date -u`) — FINAL for this lane

- Task: issue #51 / issue #2 comment 5647841253 — iPad capture-companion acceptance slice (deadline 20:28Z). Scope correction from the user (~18:55Z, relayed by the coordinator): the iPad is the capture companion (draw/capture → Mac converts to LaTeX/TikZ), not a second editor. Stop-iterating instruction received at results-15 (~19:12Z); this checkpoint closes the lane.
- Branch `agent/mac-ios-app/acceptance-slice` (from origin/agent/mac-claude-a/mac-shell 9ba9851c); worktree `/Users/jay3332/Projects/flashtex/.claude/worktrees/agent-ac31e4eddb346a6d2`. Commits: 545a6f9c (first iteration), 2da35e69 (UI test), 925acba6 (pivot), final commit = tip of the branch (evidence + last fixes). Owned paths: `apps/ios/**`, `coordination/mac-ios-app.md`, `coordination/agents/mac-ios-app.json`, `docs/evidence/ios-acceptance-2026-09-12/`. apps/mac, apps/companion and crates were never edited.
- Build: `cd apps/ios && xcodebuild -project FlashTeXPad.xcodeproj -scheme FlashTeXPad -destination 'platform=iOS Simulator,name=iPad Air 11-inch (M3)' build` → BUILD SUCCEEDED (Xcode 26.3 17C529, iOS 26.3 runtime 23D8133).
- Test: same command with `test`. Last full run results-15 (19:12–19:14Z, tree = final commit): FlashTeXPadTests 12/12 PASS (AcceptanceSliceTests 7, CaptureQueueTests 5); CaptureFlowUITests 3/3 PASS (discard-before-send 20.9 s, draw→send→receipt 24.7 s, sample-image path 15.5 s); FlashTeXPadUITests.testOpenSampleThenCancelThenApprove (.tex reference flow) PASS (50.0 s) → "Executed 4 tests, with 0 failures" at 19:15Z. No test is marked skipped.
- Simulator: iPad Air 11-inch (M3), udid 82FE2469-DE41-450C-95EF-36E6FB94D6B7, booted by this lane and shut down at the end; the pre-booted iPad Pro 13-inch (M5) belongs to someone else and was not touched. Only pids this lane launched were ended.
- Real on the wire (reference `NearbyClient` code by symlink, TLS-PSK to a FakeMac hosted in the test process / XCUITest runner): pairing-code bootstrap → `hello`/`hello_ack` (+`pair_psk`), reconnect with the stored long-term key against a restarted listener, `destination_query`, `capture_submit` (PencilKit drawing or sample image as PNG + instruction + destination copied from `hello_ack`) → `capture_received`, error replies, retry with the same `capture_id` after a disconnect. NOT carried by nearby-v1 §6 and therefore not shown and not faked: proposal ready / inserted / rejected, returned LaTeX/TikZ (the Mac's `NearbyAckMemory` answers an identical retry with the cached ack — apps/mac/Sources/FlashTeXMac/NearbyProtocol.swift ~L961 — so re-sending is not a status probe). Contract note for the Commander: carrying transfer-v1 `capture_status` (or a Mac→companion notification) over the nearby session would be the additive change needed.
- Gaps: no QR/Bonjour-browse UI (host/port/salt/fp/code typed), no camera (simulator has none; PhotosPicker + bundled sample), pairings in `PairFile` not Keychain, in-memory drafts/receipts, simulator-only signing (`CODE_SIGN_IDENTITY=-`), iPad only, no App Store packaging. XCTest `app.screenshot()` frames landscape content in a portrait canvas (right edge cropped); the `simctl` frame `simctl-191101Z-capture-prepared.png` is the full screen.
- Rules in force: author jay3332; trailers `Implementation-Agent: Claude Code subagent mac-ios-app (parent mac-claude-a, Fable)`, `Commit-Executor: git via Claude Code`, `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Y1nAv4pEnmMYXgteBoadHn`. No purchases; no Rust edits. Resource: parent's Claude Max 20x allowance (shared quota; usage unknown to this lane).

Agent / task / branch: mac-ios-app (Claude Code Fable subagent of mac-claude-a on mac-m1max-a) / iPad capture-companion acceptance slice (#51) / `agent/mac-ios-app/acceptance-slice`
State: ready_for_integration (lane stopped on the coordinator's instruction).
Deliverable: `apps/ios/` — README.md has the real-vs-not-carried table, build/test commands, gaps; evidence in `docs/evidence/ios-acceptance-2026-09-12/`.
