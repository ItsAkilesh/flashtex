# mac-ios-app handoff

## Durable checkpoint — 2026-09-12T19:04Z (actual `date -u`)

- Task: issue #51 / issue #2 comment 5647841253 — two-hour iPad slice (deadline 20:28Z). SCOPE CORRECTION received ~18:55Z from the coordinator (user verbatim: the iPad is the capture companion — draw/capture images for the Mac to translate into LaTeX/TikZ — not a second editor). Re-centred: primary screen = PencilKit canvas + photo picker + bundled sample image + instruction → `capture_submit`; per-capture status list; `.tex` panels demoted to a "Reference" sidebar section.
- Branch `agent/mac-ios-app/acceptance-slice` (pushed through 2da35e69: first iteration, .tex reference flow, 7/7 unit + 1/1 UI passing at results-8). Worktree `/Users/jay3332/Projects/flashtex/.claude/worktrees/agent-ac31e4eddb346a6d2`. Owned paths: `apps/ios/**`, `coordination/mac-ios-app.md`, `coordination/agents/mac-ios-app.json`, `docs/evidence/ios-acceptance-2026-09-12/`. Never edits apps/mac, apps/companion, crates.
- Uncommitted at this checkpoint (pivot): `apps/ios/Packages/FlashTeXPadKit/Sources/FlashTeXPadKit/CaptureQueue.swift`, `apps/ios/FlashTeXPad/CaptureView.swift`, PadModel/ContentView/App edits, `Resources/sample-capture.png`, `FlashTeXPadTests/CaptureQueueTests.swift`, `FlashTeXPadUITests/CaptureFlowUITests.swift` (+ FakeMac symlink), generator (UITests link NearbyClient, png resources), README rewrite, evidence `docs/evidence/ios-acceptance-2026-09-12/tex-reference-flow/*.png`. `build-for-testing` succeeded 18:58Z; full `xcodebuild test` (results-11) running at this checkpoint after fixing: FakeMac.restart for the long-term-key reconnect test, `capture.canvas` identifier for XCUITest drags, Editor tap in the .tex UI test.
- Verified facts driving the design: nearby-v1 §6 — no proposal/insertion outcome reaches a companion; the Mac's `NearbyAckMemory` answers an identical retry with the cached ack (apps/mac/Sources/FlashTeXMac/NearbyProtocol.swift ~L961), so "proposal ready / inserted" cannot be observed over the existing transport and is NOT faked; the iPad shows the receipt fields (`durable`, `has_proposal`, `applied` at receipt time) and a "not carried by transfer-v1" banner. Cross-process loopback TLS-PSK from the app to a FakeMac hosted in the XCUITest runner works ("Connected to Runner Mac" observed at results-10).
- Simulator booted by this lane: iPad Air 11-inch (M3) udid 82FE2469-DE41-450C-95EF-36E6FB94D6B7 (shut down at the end; the pre-booted iPad Pro 13-inch (M5) is someone else's).
- Next: read results-11 → fix any failure → copy XCTest attachments + simctl frames into docs/evidence/ios-acceptance-2026-09-12/ (capture flow: canvas drawn, prepared, received, discarded, sample image) + evidence README → commit + push → final report (real vs stubbed).
- Rules in force: author jay3332; trailers `Implementation-Agent: Claude Code subagent mac-ios-app (parent mac-claude-a, Fable)`, `Commit-Executor: git via Claude Code`, `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Y1nAv4pEnmMYXgteBoadHn`. No purchases; no Rust edits; kill only pids I launched. Resource: parent's Claude Max 20x allowance (shared quota; usage unknown to this lane).

Agent / task / branch: mac-ios-app (Claude Code Fable subagent of mac-claude-a on mac-m1max-a) / iPad capture-companion acceptance slice (#51) / `agent/mac-ios-app/acceptance-slice`
State: in_progress — pivot implemented and compiling; full simulator test run in progress.
Deliverable: `apps/ios/` (README.md has the real-vs-not-carried table, build/test commands, gaps).
Contract note for the Commander: if the iPad must show "proposal ready / inserted / returned LaTeX", nearby-v1 needs an additive message (e.g. carrying transfer-v1 `capture_status` over the companion session, or a Mac→companion notification). Not invented here.
