# mac-nearby-client handoff

- Updated UTC: 2026-09-12T08:52:00Z
- Agent / parent / machine alias: mac-nearby-client (Claude Code subagent, Opus) / mac-claude-a / mac-m1max-a
- Task / acceptance gate / owned paths: lane "Resume released 80b5f84 nearby client and harden bounded disconnect/reconnect"; follow-ups "Duplicate capture delivery and revoked destination integration tests" and "Document measured native pairing/recovery behavior". Gate: bounded reconnect with backoff + explicit terminal errors + no unbounded buffers, deterministic e2e tests against the real listener, `swift test` green in `apps/mac` and `apps/mac/tools/nearby-client`. Owned: `apps/mac/tools/nearby-client/**`, `apps/mac/Tests/FlashTeXMacTests/NearbyReferenceClientTests.swift`, the "Reference companion client" subsection of `apps/mac/README.md`, `docs/evidence/nearby-client-recovery-*.md`, this file, `coordination/agents/mac-nearby-client.json`. Also carried over from 80b5f84 (not mine, minimal): `apps/mac/Package.swift` test-only path dependency on `tools/nearby-client` — parent/Commander to accept.
- Branch / code revision / main integrated through: `agent/mac-nearby-client/reconnect` (worktree `.claude/worktrees/agent-a4c50016b414418ad`), based on `origin/agent/mac-claude-a/mac-shell` 6b43a3a; owned files brought over from `origin/agent/mac-claude-a/nearby-client` 80b5f84 (base f1bf50a). Main integrated through 2fd3026 (via mac-shell); origin/main 1457db4 fetched, not merged (no conflicts expected: main does not touch `apps/mac/tools`). HEAD: see `git log -1` (last pushed 6ee0163 + this checkpoint).
- State: ready for integration (lane + both follow-ups complete); continuing with polish only if asked.
- Ready behavior and evidence:
  - `NearbyConnection`: ≤ 8 in-flight requests (`overloaded`, nothing sent beyond), inbound line bound 1 MiB (complete or unterminated → close), all timers cancelled on completion, `.waiting` during connect classified at once (`handshakeFailed` for TLS, `unreachable` otherwise — no waiting out the timeout), `closeReason`/`isOpen` thread-safe.
  - `NearbyError`: `unreachable`, `overloaded`, `destinationChanged`, `attemptsExhausted`, `cancelled`; `isRetryable` (unreachable/closed/timeout/noMatchingMac/browseFailed only) and `needsRepair` (handshake refused, pair_mismatch, pairing_expired).
  - `NearbyReconnector` (actor): `ReconnectPolicy` (maxAttempts 5/op, 0.25 s ×2 → 4 s, ±20 % jitter, 60 s deadline, injectable sleep/random/clock), identical re-send of an unacknowledged capture (same capture_id), current-destination check before every delivery (hello_ack on a fresh connection, destination_query on a reused one), one session, no queue.
  - CLI `send`: `--attempts/--retry-delay/--max-delay/--deadline/--request-timeout`, re-browse by fp before each attempt, exit 3 re-pair / 4 budget exhausted / 5 destination changed.
  - Tests: `tools/nearby-client` 28/28 (3 consecutive runs); `apps/mac` 191 tests, 0 failures, 1 skipped (opt-in serve harness) with real compiler/pdf/bridge/edit-ledger binaries; `NearbyReferenceClientTests` 5 tests stable across 4 runs. All loopback only (FakeMac and NearbyState in tests bind `.loopback`; Bonjour still resolves them).
  - Native cross-process measurement: `docs/evidence/nearby-client-recovery-2026-09-12T084502Z.md` (17/17 steps as expected; pair 0.75 s, send 0.02–0.04 s warm / 1.04 s cold Bonjour, 3 s outage with port change recovered in 3.38 s on attempt 2, revoked pairing exit 3 in 0.48 s with no retry, Mac gone exit 4 in 0.60 s). Reproduce: `python3 apps/mac/tools/nearby-client/scripts/measure-native.py --out <md>`.
- Incomplete behavior / blockers / needs from others: pairing (bootstrap) is not retried by design (single-use code); no Keychain; no reselection UI in the CLI (exit 5 + hint); Wi‑Fi/iPad timing not measured (loopback only). No blockers. No protocol change needed from mac-nearby-transport (NearbyListener/Protocol/State untouched).
- Interface changes / consumer actions: `NearbyClient.connect(endpoint:pair:connectTimeout:helloTimeout:onLine:)` gained `helloTimeout`; `NearbyConnection.hello/destinationQuery` gained `timeout`; new public `ReconnectPolicy`, `NearbyReconnector`, `NearbyClient.reconnector(...)`, `NearbySession.isOpen`, `NearbyConnection.closeReason/isOpen/maxInFlightRequests/maxInboundLineBytes`. CLI exit code 3 now replaces 2 for a refused key (test updated). FT-004 companion can copy `Sources/NearbyClient` including `NearbyReconnector.swift`.
- Reviewed peer revisions / resulting adaptations: origin/agent/mac-claude-a/nearby-client 80b5f84 (previous worker) → resumed on mac-shell base, kept its tests, fixed FakeMac to loopback; origin/agent/mac-claude-a/mac-shell 6b43a3a (base) → no adaptation; origin/main 1457db4 → fetched, read log, no apps/mac/tools overlap; apps/mac/Sources/FlashTeXMac/Nearby{Listener,Protocol,State}.swift at 6b43a3a read for the contract (hello/ack/destination/capture_received/error codes, closeAfterFlush, adoptConnections, nonce reuse) → client mirrors them, no edits.
- Validation commands / results / artifact paths: `cd apps/mac/tools/nearby-client && swift test` → 28/28; `cd apps/mac && FLASHTEX_COMPILER=… FLASHTEX_PDF=… FLASHTEX_BRIDGE=… FLASHTEX_EDIT_LEDGER=… swift test` → 191 executed, 1 skipped, 0 failures (36.8 s); evidence md above.
- Exact deadline UTC / remaining time / integration reserve: none set for this lane (user: continue until explicit stop); parent integrates.
- ETA remaining, optimistic / likely / pessimistic / confidence: 0 / 0 / 30 min (if the parent asks for README count updates or a main merge) / high.
- Resource pool / allocation ID / maximum: parent mac-claude-a's Claude Max 20x session on mac-m1max-a (user-authorized); no API spend, no purchases; transferred crates untouched.
- Confirmed spend / estimated usage / in-flight reservation / remaining: unknown (subagent cannot read plan usage); no paid services called.
- Billing evidence / freshness / unknowns: none beyond the above.
- Child tasks and their deducted allocations: none (no subagents spawned).
- Dirty files / unpushed work / running jobs: none after this checkpoint commit; no background jobs.
- Decisions / failed approaches / linked findings: (1) `.waiting` during connect is treated as final for the attempt (fail fast; the reconnector's backoff owns the retry) — measured: refused loopback port reported in ≤ 6 ms instead of the 5–10 s timeout. (2) Per-operation budget counts delivery tries (a dropped reused session counts as a try without a dial); event attempt numbers are global dials. (3) Destination check is client-side because the in-memory inbox accepts any destination_id — transfer-v1 asks for a reselection action on a stale destination. (4) `NWListener` with plain `.tcp` parameters never became `.ready` in a test helper (unknown cause); a BSD-socket bind/close probe is used to find a closed port instead. (5) Bonjour finds loopback-only listeners on this machine, so no test opens a non-loopback port; the LAN serve mode is opt-in (`FLASHTEX_NEARBY_SERVE_LAN=1`).
- Exact next action or command: parent to review `git diff origin/agent/mac-claude-a/mac-shell...agent/mac-nearby-client/reconnect` and merge into mac-shell; if requested: merge origin/main into this branch and rerun both `swift test` commands.
- Resume reading list: this file; `apps/mac/README.md` "Reference companion client"; `apps/mac/tools/nearby-client/Sources/NearbyClient/NearbyReconnector.swift`; `apps/mac/Tests/FlashTeXMacTests/NearbyReferenceClientTests.swift`; `docs/evidence/nearby-client-recovery-2026-09-12T084502Z.md`; `apps/mac/docs/nearby-v1-proposal.md` §4, §7, §8.
