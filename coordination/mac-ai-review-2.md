# mac-ai-review-2 handoff — AI review (item 15) replenishment: cancellation / out-of-order rejection / reviewed-insertion recovery

- Agent / parent / machine: `mac-ai-review-2` (Claude Code subagent) / parent `mac-claude-a` / `mac-m1max-a`
- Task: Commander replenishment (issue #2 comment 5646989044). Current task: proposal CANCELLATION at every stage, OUT-OF-ORDER REJECTION, REVIEWED-INSERTION RECOVERY with the real assistant-context helper + real preview-controller. Follow-up 1: review history. Follow-up 2: explanation cache keyed by (diagnostic, source sha).
- Owned: `ProposalPreview.swift`, `AssistantRequestState.swift`, `ProposalPreviewTests.swift`, `AssistantRecoveryTests.swift`, the deterministic provider fixture (`Fixtures/controlled_assistant_provider.py`), new files per feature. Parent-retained files (ShellModel*, ContentView, PreviewView, FlashTeXMacApp, SourceEditorView) are never committed here.
- Branch: `agent/mac-ai-review-2/recovery` from `origin/agent/mac-claude-a/mac-shell` cd58fc2e (main c11c005 merged). Worktree `/Users/jay3332/Projects/flashtex/.claude/worktrees/agent-a358626085f62f63e`.
- Helpers used by tests (already built in the main checkout, pointed at by env vars, never rebuilt here): compiler/preview-controller (Sep 12 11:23), assistant-context (07:17), pinned scratch helper `apps/mac/build/assistant-context/...` (05:07).

## Coverage audit (mandatory first step, 16:04–16:16Z; local 12:04–12:16)

Sources read: `ProposalPreview.swift` (1511 lines), `AssistantRequestState.swift`, `ShellModel.swift` (approveProposal / editRefused / editApplied / captureRefund), `ShellModel+Bridge.swift` (approveBridgeProposal), `SourceEditorView.swift` (applyPendingEdit refusal reasons), `ProposalPreviewTests.swift` (26 tests), `AssistantRecoveryTests.swift` (6), `EditorDiagnosticsExplanationsTests.swift` (6), `InsertionTests.swift`, `SourceEditorViewTests.swift`, bridge/ledger recovery suites (names), `coordination/mac-ai-review.md`, `coordination/mac-assistant-recovery.md`, `tools/native-validation/mac-live/reports/20260912T110944Z.md` (assistant `bundled:false`; no review recovery scenario), `docs/evidence/mac-ai-review-assistant-{reviewed,approved}-2026-09-12.png`.

### Already covered (file:test)

Cancellation
- Reviewer Cancel button mid-provider, real helper, late provider reply discarded, sheet idle, no edit: `AssistantRecoveryTests.testReviewerCancelDiscardsLateProviderResultAndLeavesSheetIdle`.
- Cancellation by proposal / revision / anchor change mid-helper (probe) and mid-provider, fake doubles: `ProposalPreviewTests.testExplanationIsCancelledWhenProposalOrRevisionChangesAndLateRepliesAreDropped`; same with the real helper (expired note, helper's own stale-snapshot refusal replayed): `AssistantRecoveryTests.testDocumentChangeExpiresTheRequestAndTheHelperRefusesTheMovedSnapshot`.
- Cancellation mid-provider via `FLASHTEX_ASSISTANT_PROVIDER` env path: `ProposalPreviewTests.testProviderPathEndToEndFromEnvironmentWithLocalCommand`.
- Provider timeout + retry with a new id + stubborn late valid reply ignored: `AssistantRecoveryTests.testProviderTimeoutFromEnvironmentThenRetryStartsNewRequestAndIgnoresLateResult`.
- Helper killed mid-request (probe/prepare), relaunched next request; helper refusal at review; provider exit: `AssistantRecoveryTests.testHelperExitMidRequestIsSurfacedAndRelaunchedOnNextRequest`.
- Fake helper crash/hang/garbage/huge at probe, provider crash/hang/garbage, helper crash during approve: `ProposalPreviewTests.testHelperCrashTimeoutOrGarbageLeavesReviewUsableAndBufferUntouched`.

Out-of-order rejection
- Forged validate replies for OLDER request ids while `.prepared`; wrong-stage (validate/approve) replies while `.preparing`; helper answering another compile revision: `ProposalPreviewTests.testOutOfOrderOrMismatchedRepliesNeverChangeTheDisplayedState`.
- Late provider reply for the timed-out request after the retry started and after it finished: `AssistantRecoveryTests.testProviderTimeoutFromEnvironmentThenRetryStartsNewRequestAndIgnoresLateResult`.
- Provider answering another context refused by the real helper at review: `AssistantRecoveryTests.testHelperExitMidRequestIsSurfacedAndRelaunchedOnNextRequest` (`stale_context`).

Reviewed insertion / approval boundary
- Approval boundary across cancel/timeout/kill/expiry, explicit approve amends the proposal text only, `approveProposal` produces one pending edit: `AssistantRecoveryTests.testNoEditReachesTheDocumentWithoutExplicitApproveAcrossRecoveries`.
- Editor refuses a pending edit outside the buffer (no capture refund): `SourceEditorViewTests.testPendingEditOutsideTheBufferIsRefusedWithoutChangingText`; one undo step delivered once: `testCaptureInsertionIsOneUndoStepDeliveredToTheModelOnce`.
- Bridge path: buffer changed between prepare and apply → reselection: `ShellModelBridgeTests.testBufferChangedBetweenPrepareAndApplyIsRefusedWithReselection`; ledger/bridge crash mid-apply exactly once, durable edit survives relaunch, receipt settled once: `BridgeRecoveryTests.testLedgerCrashDuringApplyInsertsExactlyOnce`, `testDurableEditAwaitingAdoptionSurvivesABridgeRelaunch`, `testReceiptInFlightAtBridgeCrashIsSettledFromTheLedgerExactlyOnce`; real preview-controller explicit insert = one durable edit: `CaptureAcceptanceTests.testExplicitInsertRidesTheRealPreviewControllerAsOneDurableEdit`; changed anchor refused by the real bridge: `CaptureAcceptanceTests.testCaptureAgainstAChangedAnchorIsRefusedByTheRealBridgeAndNeverInserted`.

Follow-ups
- Review history: NOTHING exists (no list of past proposals with outcomes; `ShellModel.appliedCaptureIDs` is a set of ids only).
- Explanation cache: `EditorDiagnostics.ExplanationCache` is keyed by compile RESULT id (per result, 8 results), not by (diagnostic, source sha): re-explaining an unchanged diagnostic after a recompile is a new helper request. The review sheet's `explain()` has no cache at all (each Explain = new helper run).

### NOT covered before this lane (implemented here)

1. Reviewer cancel at EVERY stage with the real helper: `.probe`, `.prepare` (synchronously after launch), `.validate`/`.review` (helper stage after the provider), `.approve` (during `approveReviewedEdit`), plus sheet close (`close()` → "review closed") and proposal REJECT mid-flight; each with the late reply discarded, `recoveryLog` naming the stage, no `pendingEdit`.
2. Out-of-order: a reply for stage N-1 after stage N with the SAME request id (probe/prepare replies while at provider; provider reply while at review; review reply after `.done`); a reply carrying ANOTHER proposal's context id (second ProposalPreview for a second capture) refused as "answers another context"; replies after the reviewed edit was approved (`.approved`) and after the proposal was approved for insertion and the sheet closed.
3. Reviewed-insertion recovery on the LOCAL (AI-reviewed) path with the REAL hosted editor: approve amended proposal → editor refuses because the buffer moved (revision mismatch) → `editRefused` puts the proposal (amended latex) back at the front of the queue with the exact reason, anchor restored, `appliedCaptureIDs` cleared → re-explain against the new revision (real helper) → re-approve inserts exactly once (no duplicate on re-enqueue). Helper killed mid-`approve` (real helper, by pid) → exact reason, proposal still reviewable, no loss; real preview-controller attached so the eventual insertion is one durable edit.

## Results (2026-09-12T16:30Z; local 12:30)

Branch `agent/mac-ai-review-2/recovery` tip 0b1f5ee1 (pushed): 61d6854 coord → 65126a7c current task → 54acaaa2 follow-up 1 → be063b92 follow-up 2 → 0b1f5ee1 history env override. Labelled local application of the parent diff below: `agent/mac-ai-review-2/recovery-applied` 9710f07c (pushed; the APPLIED commit is compile/test only, never to be merged as-is).

### Current task — cancellation / out-of-order / reviewed-insertion recovery (commit 65126a7c)
Files: `apps/mac/Tests/FlashTeXMacTests/ReviewRecoveryTests.swift` (new, 3 tests), `apps/mac/Tests/FlashTeXMacTests/Fixtures/delaying_assistant_context_proxy.py` (new: per-stage delay in front of the REAL helper, byte-transparent, kills the helper on SIGTERM, logs `<pid> <op> <stage>`), `AssistantRequestState.swift` (+ `AssistantRecoveryEvent.reviewClosed(requestId:stage:)` with its note), `ProposalPreview.swift` (`close()` records `.reviewClosed` when a request is in flight; 3 lines).
- `testReviewerCancelAtEveryStageDiscardsLateRepliesAndAppliesNothing`: Cancel at probe, prepare, provider, review, approve (real helper via the proxy, controlled provider): child gone by pid, `.cancelled("cancelled by reviewer")`, log `cancelledByReviewer(stage)` then `lateReplyDiscarded(stage)`, no later stage launched, `canExplain`, model untouched each time; proxy log shows the helper stopped at exactly those stages; sheet `close()` mid-prepare → `.cancelled("review closed")` + `.reviewClosed(explain-6, .prepare)` + late reply discarded; proposal rejected mid-provider + close → queue empty, provider gone, `[.probe, .prepare, .provider]` only, nothing applied.
- `testOutOfOrderRepliesForEarlierStagesOtherProposalsAndApprovedRequestsAreRefused`: probe/prepare replies (and a forged prepare failure) while awaiting the provider → 3 stale, state unchanged, no launch; provider/review/done replies after the review landed → stale; a second `ProposalPreview` (other capture, own `explain-1`) → its context id in a well-formed `proposal_review` injected into A's review stage → `.failed("validated proposal answers another context")`, B untouched, A's real late review reply then discarded; after `approveReviewedEdit` → 10 forged replies at every stage dropped, `.approved` kept; after `approveProposal` + `close()` → dropped, `pendingEdit` is exactly the reviewer's amended text.
- `testRefusedReviewedInsertionReturnsToReviewAndReapprovalInsertsOnceThroughTheRealController` (needs FLASHTEX_PREVIEW_CONTROLLER + FLASHTEX_COMPILER; real hosted `SourceEditorView` in a never-key window, bound as ContentView binds it including `editorRevision:`): real helper review → approve → amended `% reviewed: %diag:0 inserted` → `approveProposal` stages it → a model-side text change bumps the revision → the editor refuses with `the document changed since the edit was prepared (revision 2, now 3)` → `captureNote` = "Capture cap-refused was not inserted: <reason>. It is back in the review queue; approve it again.", proposal (amended text) at the front, `appliedCaptureIDs` cleared, anchor restored, no undo step, durable revision +1 for the typed edit only → re-explained at the new revision (real helper, shadow text asserted) → helper SIGKILLed mid-approve → `.failed("helper exited (9) during approve")`, `.exited(explain-1, .helper, .approve, 9)`, proposal still queued → explain-2 + approve bound to revision 3 / the shadow SHA → second `approveProposal` → editor applies once (one undo step, `editApplied` once, no second refusal) → durable revision +2 with the reviewed LaTeX exactly once, preview compiled from it, anchor advanced → re-enqueue ignored, `approveProposal` → `.duplicate`.
Runs: `swift test --filter "ReviewRecoveryTests|ProposalPreviewTests|AssistantRecoveryTests"` with FLASHTEX_COMPILER/PREVIEW_CONTROLLER/ASSISTANT_CONTEXT = main-checkout release binaries: 34 tests, 0 failures, 2 skipped (`testProviderTimeoutFromEnvironment…` load 35 > 20; evidence PNG opt-in). ReviewRecoveryTests 3/3 also at 1-min load 46 (first run: test 3 failed only because my hosted editor omitted `editorRevision:`; fixed, 3/3 since, five runs).

### Follow-up 1 — review history (commits 54acaaa2, 0b1f5ee1)
Files: `apps/mac/Sources/FlashTeXMac/ReviewHistory.swift` (new: `ReviewHistoryEntry`, `ReviewHistory` (200 entries, 4 KiB LaTeX clip on a scalar boundary, atomic JSON with ISO-8601 dates/sorted keys, tolerant load), `ReviewHistoryRecorder` (@Observable; maps `ApproveOutcome`/reject/refused/applied; persists every decision to `~/Library/Application Support/FlashTeX/review-history/<project>.json`; `FLASHTEX_REVIEW_HISTORY_DIR` overrides the base, `off` = memory only), `ReviewHistoryView` (read-only list, newest first)), `apps/mac/Tests/FlashTeXMacTests/ReviewHistoryTests.swift` (new, 2 tests): every model outcome recorded with its exact reason from a real `ShellModel` (noAnchor, inserted, refused-back-to-review, inserted, applied, duplicate, rejected, needsReselection, declined), 9 saves, reload equals, numbering continues, view renders; bounds/clipping/corrupt/foreign-schema/missing/unwritable/env-override cases. 2/2 pass.
With the parent diff applied (recovery-applied branch) and `FLASHTEX_REVIEW_HISTORY_DIR=<scratch>`, ReviewRecoveryTests wrote `demo.json` with 22 entries including `cap-refused: inserted → refused → inserted → applied → rejected(duplicate path) → duplicate` — the real flow, no hook missed.

### Follow-up 2 — explanation memo keyed by (diagnostic, source sha) (commit be063b92)
Files: `apps/mac/Sources/FlashTeXMac/ExplanationMemo.swift` (new), `EditorDiagnostics.swift` (finished lane's file: `ExplanationCache` + `memo`, `store(_:for:result:documents:)`, `reuse(for:result:documents:)`; existing `store(_:for:)` unchanged), `apps/mac/Tests/FlashTeXMacTests/ExplanationMemoTests.swift` (new, 3 tests). Recall/miss matrix; reuse fills a new result id with no fetch and survives per-result eviction (crate-shaped reply, quick-fix edits intact); bench 200 diagnostics on 62 KB: best 0.29 ms at load 23, 0.67 ms at load 57 (bound 5 ms, load-aware skip > 20 — the bound was skipped both times, the numbers are measured). Existing `EditorDiagnosticsExplanationsTests` 5/5 (+1 FLASHTEX_EXPLAIN skip) at load 23.

### Parent diff (ShellModel.swift — NOT committed on the lane branch; applied only on `recovery-applied` 9710f07c)
See `docs/evidence/mac-ai-review-2-shellmodel-hooks-2026-09-12.diff` (exact unified diff, 69 lines, `git apply`-able on mac-shell cd58fc2e).
Notes on the diff: `reviewHistory` is `@ObservationIgnored lazy` so it can read `projectId` (a computed property) — created on first decision; `approveProposal` becomes a wrapper around the unchanged body (`approveProposalCore`) so every return path is recorded once; `editApplied` records only capture edits (`captureRefund != nil`), so quick fixes / bridge adoptions are not history entries. Optional UI: put `ReviewHistoryView(recorder: model.reviewHistory)` in the capture bar / a panel (parent-owned ContentView). With the hooks applied every test run writes to the real Application Support dir unless `FLASHTEX_REVIEW_HISTORY_DIR` is set — recommend `FLASHTEX_REVIEW_HISTORY_DIR=off` (or a scratch dir) in the test environment.

### Limitations / observations
- Load on this host during the runs was 23–80 (other agents building). No full `swift test` was run (the brief's < 15 gate was never met); the suites above are the measured evidence. Pre-existing `EditorDiagnosticsExplanationsTests.testMarksPlusExplanationsStayFastAfterFirstFetch` (not load-aware, 2 ms bound) passed at 1.07 ms (load 23) and failed at 2.03/2.07 ms (load 48/80) — unrelated to this lane's code (the memo field is untouched in that path); suggest the parent's load skip rule there.
- "Every stage" covers the helper stages probe/prepare/review/approve and the provider; the `validate` stage (explanation-only provider reply) is the same code path as `review` (`case .validate, .review`) and is exercised by the controlled provider only via review; not separately cancelled.
- Out-of-order injection uses `handleExplanationReply` directly (as the existing tests do); the reply-for-another-proposal case forges the same request id with the other preview's real context id — a real process cannot deliver a reply to another `ProposalPreview` instance, so the guard under test is the context binding, not process routing.
- The refused-insertion test moves the buffer with a model-side `updateActiveText` (the same revision bump a keystroke or paste produces); a literal keystroke through the text view is covered elsewhere (`SourceEditorViewTests`).
- No live provider; nothing here is evidence about any model.

## Checkpoint (durable)
- Branch `agent/mac-ai-review-2/recovery` @ 0b1f5ee1 (+ the final coord commit), pushed; `agent/mac-ai-review-2/recovery-applied` @ 9710f07c, pushed (labelled, compile/test only). Worktree `/Users/jay3332/Projects/flashtex/.claude/worktrees/agent-a358626085f62f63e`. Dirty files: none after the coord commit. Consumed base: origin/agent/mac-claude-a/mac-shell cd58fc2e (main c11c005).
- Next commands (if resumed): fetch origin, check out `agent/mac-ai-review-2/recovery`; `cd apps/mac && swift build --build-tests`; run the filters above with FLASHTEX_COMPILER=/Users/jay3332/Projects/flashtex/crates/compiler/target/release/flashtex-compiler FLASHTEX_PREVIEW_CONTROLLER=…/preview-controller/target/release/flashtex-preview-controller FLASHTEX_ASSISTANT_CONTEXT=…/assistant-context/target/release/flashtex-assistant-context FLASHTEX_NO_ACTIVATE=1.
- State: ready_for_integration (lane branch) + parent diff request. Resource: shared Claude Max quota with parent; no purchases, no network, no live provider.
