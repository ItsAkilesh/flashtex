{
  "agent_id": "commander-runtime-display",
  "next_index": 2,
  "schema_version": 1,
  "steps": [
    {
      "acceptance": [
        "Start from integrated decoder ecbaf7c on main; preserve four raw frames plus one decoded permit and joined shutdown",
        "Exactly one contiguous sibling only after requested and accepted nonfailed result; bind ID/project/revision/document hashes and reject malformed, unsolicited, missing or out-of-order frames",
        "Default legacy requests unchanged; preserve line limits and source epochs, no duplicate renderer parser or automatic native activation",
        "Use corrected actual producer65dbe7d or later raw-byte font hashes; old contract SHA-bytes-plus-face deviation is historical, not accepted resource evidence",
        "Publish bounded candidate API for existing helper owner; if deep validation is deferred, the type and contract must remain explicitly untrusted until existing native/core validator accepts it"
      ],
      "allocation_id": "hosted-openai-product",
      "name": "Assess malformed raw metadata allocation and propose early bounded validation without arbitrary legacy restrictions",
      "objective": "Assess malformed raw metadata allocation and propose early bounded validation without arbitrary legacy restrictions",
      "owned_paths": [
        "crates/document-runtime"
      ],
      "timebox_minutes": 40
    },
    {
      "acceptance": [
        "Start from integrated decoder ecbaf7c on main; preserve four raw frames plus one decoded permit and joined shutdown",
        "Exactly one contiguous sibling only after requested and accepted nonfailed result; bind ID/project/revision/document hashes and reject malformed, unsolicited, missing or out-of-order frames",
        "Default legacy requests unchanged; preserve line limits and source epochs, no duplicate renderer parser or automatic native activation",
        "Use corrected actual producer65dbe7d or later raw-byte font hashes; old contract SHA-bytes-plus-face deviation is historical, not accepted resource evidence",
        "Publish bounded candidate API for existing helper owner; if deep validation is deferred, the type and contract must remain explicitly untrusted until existing native/core validator accepts it"
      ],
      "allocation_id": "hosted-openai-product",
      "name": "Implement justified opt-in metadata bounds or request-aware validation with exact refusal and queue lifecycle tests",
      "objective": "Implement justified opt-in metadata bounds or request-aware validation with exact refusal and queue lifecycle tests",
      "owned_paths": [
        "crates/document-runtime"
      ],
      "timebox_minutes": 40
    },
    {
      "acceptance": [
        "Start from integrated decoder ecbaf7c on main; preserve four raw frames plus one decoded permit and joined shutdown",
        "Exactly one contiguous sibling only after requested and accepted nonfailed result; bind ID/project/revision/document hashes and reject malformed, unsolicited, missing or out-of-order frames",
        "Default legacy requests unchanged; preserve line limits and source epochs, no duplicate renderer parser or automatic native activation",
        "Use corrected actual producer65dbe7d or later raw-byte font hashes; old contract SHA-bytes-plus-face deviation is historical, not accepted resource evidence",
        "Publish bounded candidate API for existing helper owner; if deep validation is deferred, the type and contract must remain explicitly untrusted until existing native/core validator accepts it"
      ],
      "allocation_id": "hosted-openai-product",
      "name": "Measure bounded metadata residency and preserve source identity, duplicate and finite-number policies",
      "objective": "Measure bounded metadata residency and preserve source identity, duplicate and finite-number policies",
      "owned_paths": [
        "crates/document-runtime"
      ],
      "timebox_minutes": 40
    }
  ],
  "task_id": "FT-049"
}
