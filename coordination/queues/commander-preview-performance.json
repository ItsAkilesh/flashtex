{
  "agent_id": "commander-preview-performance",
  "next_index": 1,
  "schema_version": 1,
  "steps": [
    {
      "acceptance": [
        "Consume already-published index APIs without duplicate parser or index implementation",
        "Actual helper tests cover Unicode, comments/verbatim, multiple documents, stale version maps, bounded output and malformed requests",
        "No automatic source edits or project-wide atomicity claim; existing approval and permanent-ID ledger guards remain authoritative"
      ],
      "allocation_id": "hosted-openai-product",
      "name": "Expose existing project-index literal replacement and citation rename plans through exact-version guarded bounded native helper commands; preserve separate explicit durable application",
      "objective": "Expose existing project-index literal replacement and citation rename plans through exact-version guarded bounded native helper commands; preserve separate explicit durable application",
      "owned_paths": [
        "crates/preview-controller",
        "crates/document-runtime",
        "crates/edit-ledger"
      ],
      "timebox_minutes": 40
    },
    {
      "acceptance": [
        "Consume already-published index APIs without duplicate parser or index implementation",
        "Actual helper tests cover Unicode, comments/verbatim, multiple documents, stale version maps, bounded output and malformed requests",
        "No automatic source edits or project-wide atomicity claim; existing approval and permanent-ID ledger guards remain authoritative"
      ],
      "allocation_id": "hosted-openai-product",
      "name": "Apply explicitly reviewed per-document plans through existing durable grouped edits, with permanent request IDs and exact current source guards; report per-document outcomes without project-wide atomicity",
      "objective": "Apply explicitly reviewed per-document plans through existing durable grouped edits, with permanent request IDs and exact current source guards; report per-document outcomes without project-wide atomicity",
      "owned_paths": [
        "crates/preview-controller",
        "crates/document-runtime",
        "crates/edit-ledger"
      ],
      "timebox_minutes": 40
    },
    {
      "acceptance": [
        "Consume already-published index APIs without duplicate parser or index implementation",
        "Actual helper tests cover Unicode, comments/verbatim, multiple documents, stale version maps, bounded output and malformed requests",
        "No automatic source edits or project-wide atomicity claim; existing approval and permanent-ID ledger guards remain authoritative"
      ],
      "allocation_id": "hosted-openai-product",
      "name": "Verify subprocess kill/retry/stale-plan recovery and publish exact native search/history consumer handoff using existing helper boundaries",
      "objective": "Verify subprocess kill/retry/stale-plan recovery and publish exact native search/history consumer handoff using existing helper boundaries",
      "owned_paths": [
        "crates/preview-controller",
        "crates/document-runtime",
        "crates/edit-ledger"
      ],
      "timebox_minutes": 40
    }
  ],
  "task_id": "FT-048"
}
