%% Oracle sample: the runtime-v1 fixture text (protocol/fixtures/compile-request.json).
%% The preamble (everything before \begin{document}) is stripped for the FlashTeX compiler; see README.md.
\documentclass{article}
\begin{document}
Hello FlashTeX.
\end{document}
