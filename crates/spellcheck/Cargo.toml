[package]
name = "flashtex-spellcheck"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Offline, bounded, source-aware spelling suggestions. Callers supply the dictionary; math and LaTeX command ranges are excluded; zero network access, zero bundled word list."
publish = false

[lib]
name = "flashtex_spellcheck"
path = "src/lib.rs"

[dependencies]
