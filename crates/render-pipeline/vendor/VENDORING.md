# Vendored sibling crates (pinned mirrors)

These directories are byte-for-byte copies (`git archive <sha>:crates/<name>`)
of sibling crates at the revisions the pipeline is built against. Most are
task-branch tips that are **ahead of `main`**; they exist only so
`crates/render-pipeline` builds standalone from a `git archive` of this
directory alone (which is how the visual-oracle harness builds it). They are
read-only here: no edits, no fixes; requested API changes go to the owning
agent through `docs/proposals/rendering-abi.md` and are listed in the
render-pipeline README. Each directory carries a `PIN` file with the full
commit SHA it was exported from. Directory names are the plain crate names
because the siblings depend on each other by `../<name>` path.

| Directory | Branch | Commit | Owner |
| --- | --- | --- | --- |
| `compiler` | `main` | `c583d6d4` (was `dbf6ec78`; compiler pin `343e42bf` made `MathAtom.width_em` `pub` (crate-private before): `\varnothing`'s msbm10 0.777781em (`\emptyset`'s identical U+2205 stays cmsy10's 0.5em). The pipeline reads it directly at the atom (`typeset::symbol_atoms`) instead of re-scanning the source, substituting a private-use sentinel (`mathfont::VARNOTHING_SENTINEL`, never a compiler output) so the metrics providers can tell the two U+2205 occurrences apart: `TexMathMetrics::glyph`/`symbol_family_glyph` still box it at cmsy10's `\emptyset` slot 0x3B but force the advance, and `TexMathMetrics::otf_glyph` paints it from the secondary face (`NewCMMath-Regular.otf`, same as `\mathbb`/`\mathcal`) when loaded, reported once as the `lmsy10 \varnothing` resource profile, else from Latin Modern Math with the forced width unchanged; `typeset::math_items` resolves the sentinel back to U+2205 for text extraction so nothing but the metrics/painting lookup ever sees it. New test `varnothing_sets_at_msbm_width_and_paints_from_new_computer_modern_when_bundled`; `cargo test --release`: 95 passed (was 94). Also carries `fe63bd5e`/`4e8acbb0` (new `supported.rs` module, generated supported-LaTeX docs/JSON/coverage; the pipeline does not use it) and `8f6f82c3` (internal incremental-compile rewrite, no public API change); neither needed a pipeline change. HW1 (`--font-dir apps/mac/Fonts`): unchanged, 3 pages, 0 errors, 0 overfull. HW2: unchanged, 3 pages, 0 errors, 10 warnings; the pre-existing overfull line (`...nonempty sets $A,B,C\subseteq\Z$.`, 3.25pt) is untouched (width delta 0.00pt) because that line has no `\varnothing`/`\emptyset` of its own, so exact symbol widths do not move it. Older notes: was `d416472a`; `\mathcal{A-Z}` is now a `Symbol` atom of Unicode script capitals (`newcm_math::script`, the eight Letterlike Symbols exceptions included) that the compiler binds to New Computer Modern Math (`newcm_math::FONT_ID`/`SHA256`): the pipeline sets each at cmsy10's slot 0x41–0x5A (`mathtex::script_capital_slot`, TFM width/height/italic exactly as pdfLaTeX) and paints it from the already-loaded secondary face `NewCMMath-Regular.otf` (`mathfont::is_script_capital`/`is_secondary_face`; `TexMathMetrics::otf_glyph` for the TFM provider, `MathFonts::glyph` for the OpenType one), reported once as the `lmsy10 \mathcal capitals` resource profile because NewCM's script design is not cmsy's calligraphic one; without that face Latin Modern Math's script glyphs paint at the same cmsy metrics. HW2's `\mathcal` error is gone and `\mathcal{P}(T)` lands within the page's existing 0.3 pt drift of the reference. `MathAtom.width_em` (crate-private, `\varnothing` = msbm10 0.777781 em) is NOT read: the pipeline still sets `∅` at cmsy's `\emptyset` slot; re-reading the control word at the span like `class_override_of` is a follow-up. The compiler's own layout changes (display skips, `keep_line_with_display`, sectioning skips, `\vspace`/`\setlist` `em` at the class size) feed its v1 layout, which the pipeline never runs, so nothing is applied twice; `export.rs` marks the script capitals unrepresentable for the base-14 writer, which is also what the vendored `pdf` crate does (`?`), like `ℝ`/`ℤ`. amsmath corpus fixtures, `hw2_gate.rs`, `newcm_math_binding.rs` are test-only. Older notes: was `42557b09`; the compiler gained hw2-math symbols (`\subsetneq`, `\Longleftrightarrow`, `\triangle`, `\longrightarrow`, `\varnothing`, ...), `MathAtom.class_override` (crate-private: the pipeline re-reads `\mathbin`/`\mathrel`/.../`\bot`/`\bigtriangleup` from the source at the atom span, `typeset::class_override_of`), `Nucleus::Group` (the `\math<class>{...}` argument, one atom of that class), `Nucleus::SizedDelimiter` (`\left`/`\right` now paired by the compiler with roles `Left`/`Right`, mapped to math-layout `Delimited` exactly like the older source-derived fences, which stay for `Symbol` atoms; `\big`..`\Bigg` set at text size with the command's class and a `math_limitation`), amsthm, tabular, hyperref-text, verbatim, xref/toc, cite/bibliography, maketitle, page-control, footnotes, bounded-frame, justify, accent-skew, unbraced-args, diagnostic-codes and local-recovery. `Inline::Reference` gained `equation` (`\eqref`: the pipeline parenthesises the value) and `space_before` (ignored, gaps come from the source). Constructs the pipeline cannot lay out are lowered with a typed `unsupported_block` warning and never dropped (`adapter::lower_blocks`/`lower_inline`): `Block::Verbatim` -> a `flushleft` paragraph of `\\`-joined lines in the body face; `Block::TableOfContents` -> the `Contents` heading only; `Block::TitleBlock` -> centred paragraphs (title `\LARGE`, authors/date `\large` through the compiler's `TextStyle::size`); `Block::VFill` -> dropped; `Inline::Footnote` -> mark as plain text plus the note text inline; `Inline::Tabular` -> cells in reading order with `\\` between rows; `Inline::Verbatim` -> body-face text. `Block::ListItem::leftmargin`/`widest_label` are not read: the pipeline keeps its own `\list` geometry (`ListGeom`) and never runs the compiler's layout, so nothing is applied twice; `thebibliography` hangs at the ordinary list indent. The compiler's `\setlist leftmargin` and amsthm package warnings are gone; HW1 pages are byte-identical, HW2's six unknown-symbol errors are gone (`\mathcal` remains) and the glyphs paint from Latin Modern Math. Older notes: was `b38e1884`: every `\item` paragraph is now `Block::ListItem { level, label, content, extra_gap_before_pt, extra_gap_after_pt }` (`1f9bd33c`/`6c07a4f1` list-indent: the compiler's *own* layout hangs items at `\leftmargin<i>` sums and draws the label; the pipeline takes `level`/`label` and sets the geometry itself — `ListGeom` in `adapter.rs`: article's `\leftmargin<i>` or enumitem `leftmargin=*`/`<dimen>` read from `\setlist`/`\begin{..}[..]`, the label box `\hskip-\labelwidth-\labelsep \hbox to\labelwidth{\hfil label}\hskip\labelsep` on the first line, `\parskip=\parsep` for item paragraphs); the compiler's `extra_gap_*` are **not** applied: they land on whichever paragraph the next `\item`/`\end` flushes (wrong block when an item holds a display) and evaluate `em` at 12pt, so the pipeline derives `\topsep`/`\itemsep`/`\partopsep` (class `\@list<i>` + `\setlist` overrides at the class size) with `\addvspace` semantics (`Block::Paragraph::addvspace_before`). `Inline::Text`/`Inline::Math` gained `space_before` (`5a18d274`: no invented inter-word space at math/macro-splice boundaries; the lexer now swallows whitespace after a control word) — the pipeline reads interword gaps from the source bytes (`gap_has_space`, which already skips post-control-word blanks) and ignores the flag, so nothing is doubled or lost; page-1 word x unchanged. The `font-engine` pin below still satisfies it) | compiler lead |
| `font-resources` | `main` | `d5440b0` (crates/font-resources last changed by `5c89501`; shared TFM reader `tfm.rs`/`tfm_run.rs`, `required_tfm.rs`) | commander-corpus |
| `project-files` | `main` | `d5440b0` (last changed by `d92db37`; `ProjectRoot` for the rooted TFM reads) | project-files owner |
| `font-engine` | `agent/mac-font-engine/tex-fonts` | `f418238` (main's only later font-engine change, `1ff6abc0` Core14 Symbol U+2223 `afm_char` mapping, adds no API the `79986817` compiler pin needs; not carried here) | mac-font-engine |
| `paragraph-layout` | `main` (unified) | `011a7502` (was `70209e2` of `agent/mac-paragraph-layout/linebreak`; the crate on main now carries both lineages: main's Result-returning `layout_paragraph`/`ParagraphBuilder` (`LayoutError`, `HyphenatorError`), forced-break-then-discardables empty line (14c2d00c), verbatim Knuth `patterns/hyphen.tex` Liang hyphenation (`LiangHyphenator::english`) and `\discretionary`, plus the mac branch's `Lines::diagnostics`, `LineBreakParams::{hfuzz, hbadness}`, `Stats::emergency_pass_used`, `document`/`runtime_v1`/`style` modules and rev-4 glue-set goldens. The pipeline now hyphenates (`typeset::word_items`: Liang points at cluster boundaries as flagged `\hyphenpenalty` discretionaries with the face's hyphen as pre-break text, TeX's empty `\exhyphenpenalty` discretionary after explicit hyphens and en/em dashes, hyphenation only after glue and never across a font change or into a glued-on math box; fragments re-joined into one glyph run at paint time), handles the Result (`paragraph_layout_error`), and no longer drops a trailing `\\` — the crate sets TeX's empty last line (`tests/paragraph_guard.rs`). Vendored with `document-style` (path dependency); the crate's dev-dependencies on `../font-engine`/`../compiler` are not resolved for a path dependency) | mac-paragraph-layout (paused) / mac-hyphenation |
| `math-layout` | `agent/mac-math-layout/math-boxes` | `db90047` | mac-math-layout |
| `pdf` | `agent/mac-pdf/pdf-output` | `4bd8c2e` | mac-pdf |
| `document-style` | `agent/mac-document-style/style-model` (= main) | `bfc980d` | mac-document-style |
| `vector-graphics` | `agent/kabir-claude/tikz-min` | `3593209b` (FT-062: the `tikz` module — TikZ reader, xcolor, PGF bounding-box/arc/grid/to-path rules — on top of main's display primitives; dependency-free) | kabir-claude (FT-062) |

`font-engine` enables its `paragraph`/`math`/`pdf` adapter features by
default, which is why those three siblings must be present under their plain
names next to it.

`font-resources` brings `serde`, `serde_json` and `sha2` from the registry
(the first external crates in this build); everything else is path-only.

**Delete this directory and point `Cargo.toml` path dependencies at
`../<name>` once the siblings are integrated on `main`** (`main` currently
carries older revisions of font-engine, paragraph-layout, math-layout and
pdf than the pins above). Nothing binary is vendored; Latin Modern's OTFs
and TFMs are read from the local TeX Live installation at run time, never
committed; the 12 pt TFM set is digest-bound to the official Latin Modern
2.004 release (`REQUIRED_TFMS` in `src/fonts.rs`).
