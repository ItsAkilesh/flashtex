\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\setlength{\parindent}{0pt}
\pagestyle{empty}
\begin{document}
Inline $a \leq b \geq c \neq d \approx e$ text.

Display \[ a \leq b \geq c \neq d \approx e \] after.

Inline $a \equiv b \sim c \subset d \subseteq e$ text.

Display \[ a \equiv b \sim c \subset d \subseteq e \] after.

Inline $a \supset b \supseteq c \perp d$ text.

Display \[ a \supset b \supseteq c \perp d \] after.

Inline $a \in b \ni c \notin d$ text.

Display \[ a \in b \ni c \notin d \] after.

\end{document}
