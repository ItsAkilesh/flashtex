# Actual 26-page fallback through the raw helper

The release helper at `e39f50e594703c0d4b4bd6848e72fd2fe5169678`, with raw
startup/capability explicitly selected, consumes the unchanged `6e696616` producer
and the three source states from runtime `display-multipage-requests.json`.
Fixture, binary, assets and replay/client hashes are recorded. Project/request
identities are freshly assigned by this helper replay; source texts are unchanged.

Every state returns all26 v1 pages, byte-for-byte source recovery after the final
helper kill/reopen, and complete semantic equality against a fresh direct producer
request. No v1 page/item is truncated. Counts and full diagnostics are in provenance;
original direct stdout and parsed helper events/requests are retained as compressed
artifacts. There is no candidate JSONL because v2 is declined in all three states.

Each result is `recovered` with97 diagnostics, including overfull-line warnings
and an explicit `display_list_declined`. This is complete fallback evidence, not
layout correctness or successful large-v2 rendering. The separately measured
runtime artifact is25,120,854bytes, beyond existing caps; no limit was raised here.

V1 receipts were roughly146–151ms in this one correctness run while integration
checks were active. These are not native paint measurements or a calibrated speed
comparison. The default Value helper route is unchanged.

The shared harness now accepts a three-entry request sequence (including the
runtime fixture's case_id/request wrapper), with repeat=1, so original source edits
are replayed exactly rather than generated by text replacement. An initial adapter
attempt expected bare requests and stopped before helper launch; wrapper support
was added before this completed run.
