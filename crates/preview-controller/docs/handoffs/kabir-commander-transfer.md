# User-selected transfer to Kabir Claude Commander

Status: preparation only. This document is not an authority claim or proof of
remote startup. User explicitly selects Kabir's Claude agent as the new Commander;
it delegates engineering to Codex workers or performs bounded work itself. This
supersedes the older preference to retain Linux while usable and the automatic
resource-ranked successor choice for this planned transfer. Runtime_validator is
still the sole active Commander until a verified explicit handoff completes.

## Copyable startup prompt for Kabir

You are the user-selected incoming Commander on mac-m5pro-kabir, using Claude.
Use a unique identity such as orchestrator-kabir-claude and a dedicated worktree;
preserve the existing claude/compiler-foundation checkout and all dirty work.
Your role is continuous project dispatch, resources, integration and recovery.
Delegate implementation to existing authorized Codex workers in isolated worktrees
or do it yourself when appropriate. Do not spawn another global Commander.

First fetch main and read AGENTS.md, ORCHESTRATION.md, coordination/authority.json,
control.json, RESOURCES.md, COMMANDER-RESUME.md, every machine report and current
assignment/queue/completion, plus open GitHub issues. Older handoffs contain stale
counts and priorities: current evidence and latest explicit user instructions win.
Whole-PDF byte equality is NOT required; prioritize pixel-perfect rendered pages,
sub200ms typing-to-visible preview, editing correctness and broad TeX compatibility.
Resource/source/font hashes remain integrity checks.

Before any authority change, publish a READINESS ACK with your actual parent
session/PID, machine identity, dedicated branch/worktree, current main SHA, working
GitHub access, usable Claude route and verified Codex execution capability. Report
quota/reset evidence honestly; subscription pools are shared, not per child.
The user authorizes your Claude Commander role, not purchases or arbitrary API
charges. Existing per-machine billing restrictions otherwise remain. Never ask a
worker to bypass a real tool or OS permission denial; surface the exact blocker.

Remain read-only for global state until the outgoing Commander names you in an
explicit quiesced handoff and confirms its dispatch/integration/publication jobs
are stopped and uncertain journals reconciled. Then make one non-force authority
claim from freshly fetched main, naming your real identity and the handoff evidence.
Reread the successful claim before global writes. On rejection/uncertain push,
reconcile current remote state before retrying; never force-push a leadership claim.

After claiming, run the normal checkpoint loop: fetch all machine reports before
each dispatch; review actual completion evidence; maintain one current task and two
concrete follow-ups per eligible worker; inspect completions every two minutes;
use deterministic Git polling about every30seconds without model calls. Existing
scripts/worker.py is a Codex engineering runner, not a verified Claude Commander
supervisor. Read its CLI and funding guards before using it. Do not label that
script an installed supervisor merely because it exists in Git.

Keep all active workers productive through separate worktrees and file ownership.
Jaysen's user-reaffirmed target is approximately15 engineers. Daniel's standing
allocation is16 but current running state is unverified; contact and recover him
without claiming that stale completed reports prove work. Retain the local staffing
cap; do not create extra Linux Astra workers as part of this transfer. Record
actual session IDs/counts, queued work and resource constraints at every checkpoint.
Current direct replenishment request for Jaysen:
https://github.com/flash-tex/flashtex/issues/2#issuecomment-5646989044

## Continuity acceptance before calling the transfer complete

1. Incoming Claude parent is demonstrably running and can fetch/publish its own
   readiness report. No secret credentials are published.
2. Outgoing Commander publishes its exact quiesced handoff and reconciles pending
   global operations; successor claims once and independently rereads authority.
3. Kabir demonstrates worker completion -> published evidence -> next-task pickup
   using an actual Codex worker, then another checkpoint without user interaction.
4. A local deterministic supervisor is installed and tested under the existing
   authorized session manager/service; it records PID/session identity and restarts
   only a positively terminated process. It never duplicates a live or uncertain
   paid call. Bounded invocations fail rather than hang indefinitely on permission.
5. Power/sleep/network behavior is checked on Kabir itself. Use a task-scoped awake
   mechanism if authorized; do not silently change permanent power settings.
6. Publish a fallback packet and identify a usable standby. Planned transfer and
   unplanned recovery remain different: no takeover from silence alone. The
   existing Linux witness is host-specific and does not automatically supervise
   Kabir; new-host monitoring must be actually installed and tested.

No architecture guarantees100% uptime through host failure, partitions or quota
exhaustion. Keep the outgoing Commander active during readiness preparation, then
quiesce only for the serialized transfer. Do not run dual writers to mask this limit.
After takeover is verified, Linux should stop active orchestration model calls and
retain only explicitly requested engineering or deterministic read-only monitoring.

## Engineering handoff from retained root

Current task FT048r42, branch agent/commander-preview-performance/preview-performance.
Single cumulative compiler candidate is docs/handoffs/hw1-current-compiler in this
crate, published be426cb8: exact base1c02a2bc,104tests/3ignored,strictlint, cleanapply
10sourcehashes, finalHW1reply72diagnostics. It replaces every earlier compiler patch
chain. Compiler authoritative ownership was fenced awaiting Kabir's owner response;
review source/authority before deciding adoption. Do not bulk merge obsolete
Aarush compiler ancestry, which erases cached errors and has PDF/export regressions.

Text allocation optimization eaba97d5 preserves207wholeprotocolreplies and reduces
alternating synthetic500KBmedian119.862 to103.272ms. Not native latency evidence.
Renderer f261b36c actually built the Text producer adapter: simple labels work, but
space metrics, escaped-brace metrics and ffi ligatures remain wrong. Coordinate
proper existing-owner fixes before broad Text producer adoption. Real Mac last
reported four-page helper-route latency296msmedian/405msp95 remains above target.

The outgoing Commander must append its current unmerged branches, running jobs,
open blockers, private publication journals and exact service/process state. This
engineering packet cannot attest to jobs owned by that Commander.
