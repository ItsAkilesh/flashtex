# Native conformance followup

Actual parent `8844fb59` includes lane `f5c19cb`; source/evidence hashes in `source-pins.json`. This supersedes the earlier unpublished-fix status at `23596ffb`. Published lane results are 85/85 and parent post-merge report 87/87; neither was rerun here.

- **D1 implemented with request-snapshot scope.** `V2Live.sourceBindingFailure` requires nonempty declared documents and checks each declared path, exact length and SHA against captured `compiledDocuments`. The check occurs after V2Loader font/page preparation but before publication. It binds the applied request, intentionally not a buffer edited during compilation, and does not assert exact declared/caller-map membership. Failed live preparation retains the prior frame. This is useful direct-route integrity, not equivalent to Rust's fresh complete `CurrentHelper` authority at every use.
- **D2 conditional.** Admission/pre-paint gate compares decoded generation to caller `membershipGeneration` only when known. Nil skips it. The new mismatch/open-document tests cover learned generations; unconditional authoritative generation validation from the first candidate is still not established. The lane explicitly suggests an initial snapshot as a possible future improvement. Do not promote nil to trusted membership or claim the strict Rust gate is already identical.
- **D3 implemented.** V2FontStore indexes raw SHA only; obsolete face-salted spelling is refused. Tests/fixtures were changed explicitly, with identity-only fixture rebinding rather than claimed fresh producer geometry. GH31 same-byte authentication remains. The prior alias caveat now describes the older parent, not this source.
- **D6 implemented and measured by owner.** Local `requestLine` includes newline; `send` checks <=1 MiB and throws `RequestTooLarge` before its write. Exact published boundary-a 1,048,576 bytes gives durable revision2; boundary-b 1,048,577 gives error. Earlier oversized trials mislabeled “exactly” are preserved with their actual larger byte counts, not counted as the boundary. Recorded helper exits after oversized input. Bridge limits were not changed. This review inspected original records and code, not a native rerun.
- **D9 correction.** The prior static suspicion of losing a live sibling while a fixture is shown was disproved by the owner's runtime test: its compile_result replaces the fixture before the sibling. `testFixtureOnScreenDoesNotLoseTheNextLiveFrameAndAFixtureIdLineIsDropped` pins the distinction. Withdraw the static inference; no consumer fix was needed.

No route activation, fresh-source navigation promise, general memory bound, performance success or reference fidelity follows from these changes. D2 nil behavior and D1 request-versus-current semantics must remain explicit in the consolidated contract.
