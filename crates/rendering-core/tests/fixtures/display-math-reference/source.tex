% FlashTeX visual corpus fixture. The preamble below is the pdflatex form of the
% apples-to-apples oracle preamble; render_reference.sh replaces everything before
% \begin{document} with the engine-specific preamble, and render_flashtex.sh
% strips it. Only the body is the declared fixture content.
\documentclass[12pt]{article}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage[margin=1in]{geometry}
\setlength{\parindent}{0pt}
\setcounter{secnumdepth}{0}
\pagestyle{empty}
\begin{document}
Before the display.
\[
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
\]
After the display.
\end{document}
