# Independent actual reply-cap capture audit

Pinned ownerf8bb23892aa01bee3b7c436493feb824ae491cbe. The read-only
`tools/audit_reply_cap_capture.py` verifies15 compressed artifacts and records
original hashes, both pairs of diagnostic hashes,78 asset hashes per capture,
and pinned harness/client sources. No workload was executed.

All six direct v1 results equal the helper previews in full. Every producer line
fits6000 bytes including newline. Growing states accept/decline/decline; returning
to the original source accepts/declines/accepts. Declined results are recovered,
contain the5999-byte limit diagnostic, omit v2 acceptance and have no sibling or
candidate. Thus decline occurs before acceptance. Existing complete v1 pages
remain in the equality check. Compile revisions2–4 and editor revisions1–3 remain
separate; candidate source hashes/lengths/revisions and current identities match.
The final acknowledgement document exactly equals each hash-verified archived
reopened document, closing the earlier capture's missing-reopen-artifact gap.

This is the Value route, so it proves semantic body equality, not original sibling
spelling. The direct request remains reconstructed by the owner harness; helper
producer stdin was not archived. Same-session progression is supported by captured
ordered events and reviewed single-client harness, not an independently recorded
producer PID stream. No native performance, cap-sized large-frame performance,
or universal compatibility claim follows. Helper optional serialization remains
a separate output cap and may refuse expansion even when producer lines fit.
