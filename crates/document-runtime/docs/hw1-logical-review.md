# Logical-symbol standalone audit

Pinned e8234e4e. All six manifest files, exact request/result correlation,
immutable HW1 source and retained binaryef545c94771675873ec78c9cedc4bb394ffcda251b239dd59dc66c004ea29d99
verify independently. Diagnostic multiset119→112 removes exactly seven error
records with no additions or unrelated suppression. Four disjunction and three
implication items bind the same removed source spans, slicing exact `\vee` and
`\Rightarrow` bytes. All four `\mid`, two `\setminus` and one `\Longrightarrow`
unsupported errors remain; no alias or substitute is accepted by this audit.

Script audit only: no compiler execution, combined-candidate verification,
authoritative compiler change, native or PDF parity claim. Existing compiler
owner remains the adopter. Prior membership provenance gaps remain unchanged.

## Complete four-candidate handoff

Pinned1cf2fae7. Seven combined manifest hashes, archived original stdin/output,
source and preserveda10dc5d17848b24f3c548c1998d331861795c26bf03c6f42a18706b83b46b135
binary verify. Diagnostic multiset119→77 is56 removed records plus14 added explicit
hfill/normalfont errors. All35 membership/quantifier/logical items match exact
command spans; four mid/two setminus/one Longrightarrow errors remain unchanged.

The hash-bound clean-apply record identifies base1c02a2bc and the same eight tested
source hashes as the combined manifest. This audit verifies that evidence's
internal agreement, not a new patch application. The authoritative handoff is
combined patcha4829c5b7d6cf94932ee18c5586989f2f8314b5c237a4f892553d6c0a1f317f1;
do not layer overlapping individual patches on top. Owner97-test/lint evidence
remains separate from this read-only audit. No authoritative adoption, rerun,
whole-PDF/native claim; standalone membership limitations remain historical facts.
