# Logical-symbol standalone audit

Pinned e8234e4e. All six manifest files, exact request/result correlation,
immutable HW1 source and retained binaryef545c94771675873ec78c9cedc4bb394ffcda251b239dd59dc66c004ea29d99
verify independently. Diagnostic multiset119→112 removes exactly seven error
records with no additions or unrelated suppression. Four disjunction and three
implication items bind the same removed source spans, slicing exact `\vee` and
`\Rightarrow` bytes. All four `\mid`, two `\setminus` and one `\Longrightarrow`
unsupported errors remain; no alias or substitute is accepted by this audit.

Script audit only: no compiler execution, combined-candidate verification,
authoritative compiler change, native or PDF parity claim. Existing compiler
owner remains the adopter. Prior membership provenance gaps remain unchanged.
