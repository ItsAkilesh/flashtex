[package]
name = "flashtex-document-style"
version = "0.1.0"
edition = "2024"
description = "Original FlashTeX document style model: page geometry, font size tables, block style inheritance, and article-class spacing, with a hand-written JSON form."
publish = false

[lib]
name = "flashtex_document_style"
path = "src/lib.rs"

[dependencies]
